# Ролевая модель и параметризация

Сожержание:

1. [Ролевая модель](https://app.notion.com/p/bf8deec4b58582698dde01f2b8f27b55?pvs=21)
2. [Параметризация](https://app.notion.com/p/bf8deec4b58582698dde01f2b8f27b55?pvs=21)
3. [Нотация и аннотация](https://app.notion.com/p/bf8deec4b58582698dde01f2b8f27b55?pvs=21)
4. .[env](https://app.notion.com/p/bf8deec4b58582698dde01f2b8f27b55?pvs=21)
5. [Практика](https://app.notion.com/p/bf8deec4b58582698dde01f2b8f27b55?pvs=21)

### Ролевая модель

### **Ролевая модель для Cinescope**

<aside>
💡

Цель: Централизовать и абстрагировать логику HTTP-запросов, связанных с управлением пользователями и ролями. Это позволяет выполнять тесты под разными пользователями с различными правами

</aside>

---

### Делаем модель юзера и создаем фикстуру супер админа

<aside>
🛠

Мы внедрим ролевую модель, используя **фабричный метод + инъекцию зависимостей**, чтобы тестировать различные роли пользователей.

</aside>

### Посмотрим на наш сваггер. Нас будет интересовать /user

### Сваггер

https://auth.dev-cinescope.coconutqa.ru/swagger#/ - здесь мы будем смотреть на раздел “Пользователь”

![image.png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image.png)

Внутри каждого метода есть указание ролей с доступом к той или иной ручке (ендпоинту)

![image.png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image%201.png)

---

### Креды юзера с правами супер-админа

<aside>
➖

Суперадмин нам понадобится, чтобы взаимодействовать с этой апишкой в полной мере. На проектах как правильно всегда есть тестовый юзер с полными правами. Создание их будет различаться.

</aside>

`SUPER_ADMIN_USERNAME = 'api1@gmail.com'`
`SUPER_ADMIN_PASSWORD = 'asdqwe123Q'`

---

### **Создадим модель юзера**

- 📌 `Modul_4/Cinescope/entities/user.py`

```python
from Modul_4.Cinescope.api.api_manager import ApiManager

class User:
    def __init__(self, email: str, password: str, roles: list, api: ApiManager):
        self.email = email
        self.password = password
        self.roles = roles
        self.api = api  # Сюда будем передавать экземпляр API Manager для запросов

    @property
    def creds(self):
        """Возвращает кортеж (email, password)"""
        return self.email, self.password
```

<aside>
❓

**Зачем это нужно?**

1. Описывает структуру пользователя. Мы можем в дальнейшем добавить новых параметров или добавить аттрибутов объектам и в тесте у нас будет объект текущего юзера (а точнее нескольких юзеров раздельно, чтобы не путать где какой)
2. Свойство creds позволит нам более удобно пользоваться аутентификацией юзера
3. Включает `api_manager` для выполнения API-запросов. То есть теперь мы будем отправлять запросы через юзера, у которого есть объект апи менеджера
</aside>

---

### Пояснения по классу

### **1. `__init__`**

```python

def __init__(self, email: str, password: str, roles: list, api_manager):
```

<aside>
⚙

Этот метод вызывается при создании нового объекта `User`.

- `email: str` → строка с email пользователя.
- `password: str` → строка с паролем.
- `roles: list` → список ролей пользователя (например, `["USER"]`).
- `api_manager` → экземпляр `ApiManager`, который позволяет пользователю делать API-запросы.

Когда создаем пользователя, например:

</aside>

```python
user = User(email="test@example.com", password="123456", roles=["USER"], api_manager=api_manager)
```

<aside>
☝

Объект `user` будет содержать всю эту информацию.

</aside>

---

### **2. `@property` – Свойство `creds`**

```python
@property
def creds(self):
    """Возвращает кортеж (email, password)"""
    return self.email, self.password
```

<aside>
⚙

**Декоратор `@property`** делает метод `creds` доступным как атрибут:

</aside>

```python

print(user.creds)  # ('test@example.com', '123456')
```

<aside>
⚙

Это удобно, так как не нужно вызывать метод, а можно просто обращаться к атрибуту:

</aside>

```python
email, password = user.creds
```

---

### Добавим фикстуру на создание сессии юзера в `conftest` **и доработаем `ApiManager`**

```python
@pytest.fixture
def user_session():
    user_pool = []

    def _create_user_session():
        session = requests.Session()
        user_session = ApiManager(session)
        user_pool.append(user_session)
        return user_session

    yield _create_user_session

    for user in user_pool:
        user.close_session()
```

<aside>
⚙

1. **Создание пула сессий** (**`user_pool`**):
    
    Список для хранения всех созданных в тесте сессий
    
2. **Фабричная функция** (**`_create_user_session`**):
    - Создает новую сессию **`requests.Session()`**
    - Инициализирует объект **`ApiManager`** (Это мы его в последствии будем передавать юзеру)
    - Добавляет сессию в **`user_pool`** Этот список сессий нам понадобится для того, что бы после `yield` в конце теста закрыть все сессии
3. **Возврат фабрики** :
    
    Запрашиваемая фикстура или тест получают доступ к **`_create_user_session`**, чтобы создавать сессии по требованию
    
4. **Закрытие сессий** (после теста):
    
    Все сессии из **`user_pool`** закроются, даже если тест упал с ошибкой
    
    Для этого нам понадобится доработать api_manager, добавив в него метод закрытия сессии. Сделаем это!
    
</aside>

<aside>
➖

Добавьте в класс ApiManager новый метод, который закрывает переданную в него сессию, чтобы фикстура выше работала:

</aside>

```python
    def close_session(self):
        self.session.close()
```

### **Назначение фикстуры `user_session()`**

<aside>
💡

Фикстура **`user_session()`** управляет жизненным циклом пользовательских сессий для API-тестов. Она позволяет:

- Создавать новые сессии (объекты **`requests.Session`**) для разных пользователей
- Автоматически закрывать все созданные сессии после завершения теста (Еще раз - мы созданные сессии юзера при каждом обращении к фикстуре кладем в список. А в конце - проходясь по каждой сессии в списке их закрываем!)
- Изолировать юзеров внутри теста друг от друга (каждый работает с независимыми сессиями)
</aside>

---

### А что по принципам ООП? Ну тут все просто:

<aside>
💡

- **Инкапсуляция**:
    
    Логика создания и удаления сессий скрыта внутри фикстуры. Тесты не будут знать, как это работает — они только будут вызывать создание юзера, который в свою очередь примет в себя сессию через  **`_create_user_session()`**.
    
- **DRY (Don’t Repeat Yourself)**:
    
    Не нужно писать код для создания и закрытия сессий в каждом тесте — это делает фикстура
    
- **Управление ресурсами:**
    
    Гарантирует, что все сессии будут корректно закрыты, даже при падении теста
    
- **Гибкость** :
    
    Можно создать сколько угодно юзеров с разными сессиями внутри одного теста (а то и несколько сессий для 1 юзера!)
    
</aside>

---

### Напишем фикстуру для создания юзера админа

### Положим креды в отдельный модуль

<aside>
➖

Поместим наши креды в отдельное место. Создадим папку resources, а внутри файл user_creds.py

1. Создайте класс SuperAdminCreds, который будет в себе содержать USERNAME и PASSWORD юзера с ролью супер-админа, который был предоставлен выше. Продублирую:
    
    ```
    USERNAME = 'api1@gmail.com'
    PASSWORD = 'asdqwe123Q'
    ```
    
</aside>

---

### Реализовываем фикстуру супер-админа

<aside>
🛠

Сделаем фикстуру, которая будет создавать юзера, используя раннее написанную модель User:

- не забудьте импортировать нужные классы в `conftest`
</aside>

```python
@pytest.fixture
def super_admin(user_session):
    new_session = user_session()

    super_admin = User(
        SuperAdminCreds.USERNAME,
        SuperAdminCreds.PASSWORD,
        "[SUPER_ADMIN]",
        new_session)

    super_admin.api.auth_api.authenticate(super_admin.creds)
    return super_admin
```

<aside>
❓

### “Что делает?”

Фикстура super_admin создает и настраивает объект пользователя с ролью суперадминистратора , который используется в тестах для выполнения административных действий (например, создание других пользователей, управление ролями и тд)

Этот суперадмин нам пригодится, чтобы создавать новых юзеров или менять им роли

</aside>

<aside>
📢

### На вопрос “Зачем нужна отдельная фикстура на это?” отвечаем:

- Упрощает доступ к суперадмину в тестах. Достаточно просто передать фикстуру и можно через него сразу пользоваться апишкой
- Автоматически аутентифицирует суперадмина. Тест становится более читаемым, поскольку нет вызова логинов, кред и тд.
</aside>

<aside>
🛠

### Как работает:

1. **Создание новой сессии** :
    - Вызывается фикстура **`user_session`**, которая создает новую сессию через **`requests.Session()`** и добавляет её в пул сессий
2. **Инициализация объекта `User`** :
    - Создается объект **`User`** с параметрами:
        - Имя пользователя (**`SuperAdminCreds.USERNAME` )**
        - Пароль (**`SuperAdminCreds.PASSWORD`**)
        - Роль (**`Roles.SUPER_ADMIN.value`**)
        - Сессия (**`new_session` ) -** по факту это уже инициализированный объект ApiManager с готовой сессией, через который мы работаем с апи
3. **Аутентификация** :
    - Вызывается метод **`authenticate()`** из **`auth_api`**, который выполняет вход суперадмина и сохраняет токен аутентификации в заголовки сессии
4. **Возврат объекта** :
    - Готовый объект **`super_admin`** возвращается в тест для использования
</aside>

### Пример использования:

![image.png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image%202.png)

<aside>
➖

Теперь через аттрибут api мы сможем обращаться к нашим апи классам и вызывать их методы!

Однако нам осталось теперь создать новый API-класс UsersApi и добавить методы работы с ним. Приступим!

</aside>

---

### **Сверимся, что MoviesAPI выглядит примерно так:**

<aside>
⚙

**Файл:** `movies_api.py`

Вы его делали самостоятельно на экзамене прошлого модуля.
Не стоит его переписывать как представлено здесь. Вы работаете с ним и адаптируете его под новую логику из уроков самостоятельно, это важная часть.

Ниже лишь примеры для понимания взаимодействий.

</aside>

```python
from custom_requester.custom_requester import CustomRequester

class MoviesAPI(CustomRequester):
    MOVIE_BASE_URL = "https://api.dev-cinescope.f5qa.ru/"

    def __init__(self, session):
        self.session = session
        super().__init__(session, self.MOVIE_BASE_URL)

    def get_movies(self, params=None):
        return self.send_request("GET", "movies", params=params)

    def get_movie(self, movie_id, expected_status=200):
        return self.send_request(
            method="GET",
            endpoint=f"movies/{movie_id}",
            expected_status=expected_status
        )

    def create_movie(self, data, expected_status=201):
        return self.send_request(
            method="POST",
            endpoint="movies",
            data=data,
            expected_status=expected_status
        )

    def delete_movie(self, movie_id, expected_status=200):
        return self.send_request(
            method="DELETE",
            endpoint=f"movies/{movie_id}",
            expected_status=expected_status
        )

```

---

### Начинаем работу с api_users

### Сваггер

https://auth.dev-cinescope.coconutqa.ru/swagger#/ - здесь мы будем смотреть на раздел “Пользователь”

![image.png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image.png)

<aside>
➖

Внутри каждого метода есть указание ролей с доступом к той или иной ручке (ендпоинту)

</aside>

![image.png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image%201.png)

---

### Сделаем новый апи-класс UserApi

```python
from custom_requester.custom_requester import CustomRequester

class UserApi(CustomRequester):
    USER_BASE_URL = "https://auth.dev-cinescope.f5qa.ru/"

    def __init__(self, session):
        self.session = session
        super().__init__(session, self.USER_BASE_URL)

    def get_user(self, user_locator):
        return self.send_request("GET", f"user/{user_locator}")

    def create_user(self, user_data, expected_status=201):
        return self.send_request(
            method="POST",
            endpoint="user",
            data=user_data,
            expected_status=expected_status
        )
```

<aside>
➖

Сделаем для начала пару методов - создание и получение юзера по локатору (может быть id, может быть email)

</aside>

---

### Сделаем фикстуру для создания юзера

<aside>
💡

Дело в том, что наша прошлая фикстура по созданию тестовых данных юзера не совсем подходит. Состав тела запроса для создания юзера через POST /user содержит в себе два дополнительных параметра:

</aside>

```python
  "verified": true,
  "banned": false
```

<aside>
➖

Пока что мы не будем вдаваться в подробности за что эти параметры отвечают, а попробуем сделать тест с захардкоженными значениями

Напишем новую фикстуру, которая обновит дату из старой и вернет новый словарь:

</aside>

```python
@pytest.fixture(scope="function")
def creation_user_data(test_user):
		updated_data = test_user.copy()
    updated_data.update({
        "verified": True,
        "banned": False
    })
    return updated_data
```

Не забываем, что словарь - это изменяемый тип данных, поэтому копируем в переменную.
Если в тесте бы мы использовали сразу 2 фикстуры `test_user` и `creation_user_data` то они будут абсолютно одинаковыми

---

### Пишем тесты на создание и получение юзера

```python

class TestUser:

    def test_create_user(self, super_admin, creation_user_data):
        response = super_admin.api.user_api.create_user(creation_user_data).json()

        assert response.get('id') and response['id'] != '', "ID должен быть не пустым"
        assert response.get('email') == creation_user_data['email']
        assert response.get('fullName') == creation_user_data['fullName']
        assert response.get('roles', []) == creation_user_data['roles']
        assert response.get('verified') is True

    def test_get_user_by_locator(self, super_admin, creation_user_data):
        created_user_response = super_admin.api.user_api.create_user(creation_user_data).json()
        response_by_id = super_admin.api.user_api.get_user(created_user_response['id']).json()
        response_by_email = super_admin.api.user_api.get_user(creation_user_data['email']).json()

        assert response_by_id == response_by_email, "Содержание ответов должно быть идентичным"
        assert response_by_id.get('id') and response_by_id['id'] != '', "ID должен быть не пустым"
        assert response_by_id.get('email') == creation_user_data['email']
        assert response_by_id.get('fullName') == creation_user_data['fullName']
        assert response_by_id.get('roles', []) == creation_user_data['roles']
        assert response_by_id.get('verified') is True

```

<aside>
➖

Тесты после запуска должны быть приятного зеленого цвета, что не может не радовать

</aside>

![image.png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image%203.png)

<aside>
➖

Это наша отправная точка для написания фикстуры, которая нам будет сразу возвращать юзера с нужной нам ролью в тест. Приступим к ее написанию!

</aside>

---

### **Фикстуры для пользователей с разными ролями**

<aside>
🛠

Создадим **фикстуры** в `conftest.py`, чтобы тесты могли получать пользователей с разными ролями.

</aside>

### Реализуем создание обычного юзера с ролью USER

```python
@pytest.fixture
def common_user(user_session, super_admin, creation_user_data):
    new_session = user_session()

    common_user = User(
        creation_user_data['email'],
        creation_user_data['password'],
        list(Roles.USER.value),
        new_session)

    super_admin.api.user_api.create_user(creation_user_data)
    common_user.api.auth_api.authenticate(common_user.creds)
    return common_user
```

<aside>
➖

Довольно простая фикстура, похожа на фикстуру `super_admin` , из нового тут:

1. Мы уже получаем авторизованного супер-админа, используя фикстуру `super_admin`
2. Мы создаем юзера Супер админом при помощи нового метода `create_user()`
3. После, созданный `common_user` аутентифицируется в системе, его заголовок обновляется токеном и этим юзером с ролью “юзер” мы можем пользоваться в наших тестах
</aside>

---

### **Используем роль в тесте**

<aside>
🛠

Теперь в тестах можно **создавать пользователей с нужными ролями**.

</aside>

<aside>
➖

Обратим еще раз внимание на наш сваггер. В нем мы увидим роли, которым доступна данная апишка.

На примере **GET `/user/{idOrEmail}`** мы видим, что данный метод доступен только ролям **ADMIN** и **SUPER_ADMIN**

</aside>

![image.png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image%204.png)

<aside>
➖

Попробуем написать тест, в котором запрос будет происходить от юзера с ролью **USER**
Мы будем ожидать ответ от сервера 403 forbidden

В `test_user.py` добавим еще 1 тест:

</aside>

```python
    def test_get_user_by_id_common_user(self, common_user):
        common_user.api.user_api.get_user(common_user.email, expected_status=403)
 
```

![image (1).png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image_(1).png)

<aside>
⚠️

Обратите внимание насколько удобно теперь писать тесты и насколько они могут быть лаконичными. Все что нам нужно, это указать юзера, от роли которого мы будем отправлять запросы

</aside>

---

### **Создадим Enum для ролей пользователей**

<aside>
🛠

В файле `constants/roles.py` создадим **перечисление** всех ролей, доступных в системе

</aside>

```python
from enum import Enum

class Roles(Enum):
    USER = "USER"
    ADMIN = "ADMIN"
    SUPER_ADMIN = "SUPER_ADMIN"
```

<aside>
❓

**Зачем это нужно?**

1. Удобно использовать в тестах (`Roles.ADMIN.value`)
2. Исключает опечатки при указании ролей
3. Легко расширить, если появятся новые роли
</aside>

---

### 🔍 Детальное объяснение: Enum в Python

<aside>
⚙

В Python `Enum` (перечисление) — это специальный класс, который позволяет **определять набор именованных констант**. Это полезно, когда нужно создать **фиксированный список значений**, в нашем случае это роли пользователей

</aside>

<aside>
❓

Как работает `Enum`?

1. **Наследуемся от `Enum`**, чтобы создать перечисление
2. **Определяем константы**
3. **Каждой константе присваивается строковое значение**, которое можно использовать в коде
</aside>

> Разбор кода:
> 
> 
> 1️⃣ **`class Roles(Enum):`**
> 
> - Создаёт новый класс, наследуемый от `Enum`.
> - `Roles` становится перечислением, внутри которого хранятся фиксированные роли.
> 
> 2️⃣ **`USER = "USER"` и другие роли**
> 
> - Каждая переменная внутри `Enum` — это экземпляр класса `Roles`
> - Значение `USER = "USER"` означает, что `Roles.USER.value` вернёт строку `"USER"`.
> 
> 3️⃣ **Доступ к значениям**
> 
> - Можно обращаться к роли как `Roles.ADMIN`, а её значение получить через `Roles.ADMIN.value`
> - Можно перебирать все роли в `Roles` с помощью `list(Roles)`

---

### Примеры использования:

### 1. Получение значения роли:

```python
print(Roles.ADMIN.value)  # Выведет: "ADMIN"
```

### 2. Сравнение ролей:

```python
user_role = "ADMIN"

if user_role == Roles.ADMIN.value:
    print("Пользователь — администратор!")
```

### 3. Перебор всех ролей:

```python
for role in Roles:
    print(role.name, "->", role.value)

# Вывод:
# USER -> USER
# ADMIN -> ADMIN
# SUPER_ADMIN -> SUPER_ADMIN
```

---

<aside>
♻️

Отрефачим наш код, чтобы использовать это перечисление:

Не забудьте импортировать класс в coftest

в файле `conftest` исправим фикстуры, создающие юзера:

</aside>

```python
@pytest.fixture
def super_admin(user_session):
    new_session = user_session()

    super_admin = User(
        SuperAdminCreds.USERNAME,
        SuperAdminCreds.PASSWORD,
        [Roles.SUPER_ADMIN.value],
        new_session)

    super_admin.api.auth_api.authenticate(super_admin.creds)
    return super_admin
```

```python
@pytest.fixture
def common_user(user_session, super_admin, creation_user_data):
    new_session = user_session()

    common_user = User(
        creation_user_data['email'],
        creation_user_data['password'],
        [Roles.USER.value],
        new_session)

    super_admin.api.user_api.create_user(creation_user_data)
    common_user.api.auth_api.authenticate(common_user.creds)
    return common_user
```

```python
@pytest.fixture
def test_user():
    random_password = DataGenerator.generate_random_password()

    return {
        "email": DataGenerator.generate_random_email(),
        "fullName": DataGenerator.generate_random_name(),
        "password": random_password,
        "passwordRepeat": random_password,
        "roles": [Roles.USER.value]
    }
```

---

### 🧪Практическое задание

<aside>
🍄

1. Отрефакторить код написанный ранее под использование новых фикстур (например тесты movies)
2. Сделать тест на movies с ролью, у которой нет доступа (USER). Достаточно будет одного негативно-позитивного теста, что пользователь с ролью USER не сможет создать фильм.
    - Чтобы отправлять запросы от юзера с ролью USER, используйте фикстуру `common_user`
3. Самостоятельно написать фикстуру, по примеру как сделана `common_user` , которая будет возвращать юзера с ролью ADMIN
*(SUPER_ADMIN и ADMIN - разные роли)
</aside>

---

### Параметризация

## **Что такое параметризация?**

<aside>
💡

Параметризация – это **способ автоматического повторного запуска одного и того же теста** с разными входными данными. Это позволяет:

1. **Избежать дублирования кода**
2. **Проверять один и тот же функционал с разными данными**
3. **Повысить покрытие тестов, варьируя параметры**

Вместо того чтобы писать отдельные тесты для каждой комбинации данных, мы можем использовать **`@pytest.mark.parametrize`**, чтобы передать в тест **разные значения**.

</aside>

---

### Декоратор `@pytest.mark.parametrize`

<aside>
⚙

Основным инструментом для параметризации тестов является декоратор `@pytest.mark.parametrize`. Его синтаксис:

</aside>

```python
@pytest.mark.parametrize("param_name", [value1, value2, ...])

```

<aside>
➖

Или для нескольких параметров:

</aside>

```python
@pytest.mark.parametrize("param1,param2,...", [(value1_1, value1_2, ...), (value2_1, value2_2, ...), ...])

```

<aside>
⚙

### Технические особенности работы декоратора:

1. **Первый аргумент** — строка с именами параметров (через запятую без пробелов)
2. **Второй аргумент** — список значений или список кортежей значений
3. Pytest создаст отдельный тест для каждого набора параметров
4. Параметры передаются в тестовую функцию в том же порядке, в котором они указаны в декораторе
</aside>

---

### Посмотрим как это работает на практике на простом примере

<aside>
🛠

Рассмотрим простой пример параметризованного теста:

</aside>

```python
import pytest

@pytest.mark.parametrize("input_data,expected", [(1, 2), (2, 4), (3, 6)])
def test_multiply_by_two(input_data, expected):
    assert input_data * 2 == expected

```

<aside>
☝

В этом примере:

- Мы параметризируем тест с двумя параметрами: `input` и `expected`
- Тест запустится три раза с разными наборами данных: (1, 2), (2, 4), (3, 6)
- Каждый запуск теста будет отображаться как отдельный тест в отчете

Попробуйте его выполнить и посмотреть на результат, обратите внимание, что Pytest автоматически генерирует название теста, включая переданные параметры!

</aside>

---

### Нюансы и частые ошибки

### 1. Синтаксис строки параметров

<aside>
✅

**Правильно**: имена параметров записываются через запятую без пробелов:

</aside>

```python
@pytest.mark.parametrize("param1,param2", [(1, 2), (3, 4)])

```

<aside>
🚫

**Неправильно**: имена параметров с пробелами:

</aside>

```python
@pytest.mark.parametrize("param1, param2", [(1, 2), (3, 4)])  # Вызовет ошибку!

```

---

### 2. Несоответствие количества параметров

<aside>
✅

Количество параметров в строке должно соответствовать количеству значений в кортежах:

</aside>

```python
# Правильно:
@pytest.mark.parametrize("a,b,c", [(1, 2, 3), (4, 5, 6)])

# Неправильно:
@pytest.mark.parametrize("a,b", [(1, 2, 3), (4, 5)])  # Ошибка!

```

---

### 3. Использование одного параметра

<aside>
⚠️

Если у вас только один параметр, все равно нужно использовать список значений, а не один элемент:

</aside>

```python
# Правильно:
@pytest.mark.parametrize("user_id", [1, 2, 3])

# Неправильно:
@pytest.mark.parametrize("user_id", 1)  # Ошибка!

```

---

### Параметризация тестовых классов

<aside>
💡

Параметризация не ограничивается только отдельными тестовыми функциями. В pytest можно параметризовать целые тестовые классы, что очень удобно, когда нужно выполнить серию тестов с одинаковыми входными данными. Рассмотрим это на схематических примерах

</aside>

### Основы параметризации тестовых классов

<aside>
🛠

Для параметризации целого класса используется декоратор `@pytest.mark.parametrize` на уровне класса:

</aside>

```python
import pytest

@pytest.mark.parametrize("parameter_name", ["value1", "value2"])
class TestParametrizedClass:
    def test_first(self, parameter_name):
        print(f"Тест 1 прогон: {parameter_name}")
        assert True

    def test_second(self, parameter_name):
        print(f"Тест 2 прогон: {parameter_name}")
        assert True

```

<aside>
☝

В этом примере каждый метод внутри класса `TestParametrizedClass` будет запущен дважды - один раз с `parameter_name="value1"` и один раз с `parameter_name="value2"`.

</aside>

<aside>
🍄

Попробуйте это выполнить этот код и посмотреть результат!
можете еще и с дебаггером попробовать

</aside>

<aside>
🛠

## Как это работает технически

Когда применяется декоратор `@pytest.mark.parametrize` к классу:

1. pytest создает несколько экземпляров класса (по одному для каждого набора параметров)
2. Каждый метод теста в классе получает доступ к соответствующим параметрам
3. Параметры становятся доступными как аргументы тестовых методов
4. Каждый тестовый метод запускается отдельно для каждого набора параметров
</aside>

---

### Пример с несколькими параметрами

```python
import pytest

@pytest.mark.parametrize("param_a,param_b", [
    ("a1", "b1"),
    ("a2", "b2")
])
class TestMultipleParams:

    def test_params_combination(self, param_a, param_b):
        print(f"1 тест: {param_a} и {param_b}")

    def test_another_method(self, param_a, param_b):
        combined = f"{param_a}-{param_b}"
        print(f"2 тест: {combined}")
        assert len(combined) > 2

```

---

### Комбинирование параметризации классов и методов

<aside>
🛠

Вы можете комбинировать параметризацию на уровне класса и метода:

</aside>

```python
import pytest

@pytest.mark.parametrize("class_param", ["c1", "c2"])
class TestCombinedParametrization:

    @pytest.mark.parametrize("method_param", ["m1", "m2", "m3"])
    def test_combination(self, class_param, method_param):
        # Этот тест запустится 6 раз (2 параметра класса × 3 параметра метода)
        print(f"Тест 1 с параметризацией класса={class_param} и метода={method_param}")
        assert True

    def test_only_class_param(self, class_param):
        # Этот тест запустится 2 раза (только с параметрами класса)
        print(f"Тест 2 с параметризацией только класса={class_param}")
        assert True

```

<aside>
☝

В этом примере:

- `test_combination` будет запущен 6 раз (все комбинации параметров)
- `test_only_class_param` будет запущен 2 раза (только с параметрами класса)
</aside>

---

### Пример с пропуском тестов на основе параметров

```python
import pytest

@pytest.mark.parametrize("feature_flag,platform", [
    ("feature_a", "windows"),
    ("feature_a", "mac"),
    ("feature_b", "windows"),
    pytest.param("feature_b", "mac", marks=pytest.mark.skip(reason="Not supported on Mac"))
])
class TestFeatures:

    def test_feature_availability(self, feature_flag, platform):
        print(f"Testing {feature_flag} on {platform}")
        assert True

```

---

### **Использование `ids` для читаемого вывода**

<aside>
⚙

По умолчанию Pytest отображает параметры как `[value1-value2-value3]`.

Можно задать **читаемые имена** для тестов с `ids`:

</aside>

```python
import pytest
from resources.user_creds import SuperAdminCreds

@pytest.mark.parametrize("email, password, expected_status", [
    (f"{SuperAdminCreds.USERNAME}", f"{SuperAdminCreds.PASSWORD}", (200, 201)),
    ("test_login1@email.com", "asdqwe123Q!", 500),  # Сервис не может обработать логин по незареганному юзеру
    ("", "password", 500),
], ids=["Admin login", "Invalid user", "Empty username"])
def test_login(email, password, expected_status, api_manager):
    login_data = {
        "email": email,
        "password": password
    }
    api_manager.auth_api.login_user(login_data=login_data, expected_status=expected_status)

```

<aside>
➖

**Теперь вывод тестов будет выглядеть так**:

</aside>

![image (2).png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image_(2).png)

---

### 🧪Практическое задание

<aside>
🍄

1. Написать тест на получение всех фильмов по фильтру и сделать его параметризированным
    
    ![image.png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image%205.png)
    
</aside>

---

### Нотация или аннотация

---

### **Аннотации в Python: зачем они нужны и как работают?**

<aside>
💡

Аннотации в Python – это **специальные подсказки для программиста и инструментов**, которые показывают, какие **типы данных** ожидаются в аргументах функций и что функция должна вернуть. **Python не заставляет их соблюдать**, но они делают код **понятнее, безопаснее и удобнее**.

Давай разберёмся, зачем они нужны, как работают и в чём их польза.

</aside>

---

### **Что такое аннотации и зачем они нужны?**

<aside>
💭

Представь, что у тебя есть функция:

</aside>

```python
def add(a, b):
    return a + b

```

<aside>
❓

Какой тип данных ожидается у `a` и `b`? 

Это могут быть:

- **Целые числа** (`int`) → `add(2, 3) # 5`
- **Строки** (`str`) → `add("Hello", " World") # "Hello World"`
- **Списки** (`list`) → `add([1, 2], [3, 4]) # [1, 2, 3, 4]`

Но если кто-то случайно передаст число и строку?

</aside>

```python
add(5, "hello")  # TypeError: unsupported operand type(s)
```

<aside>
☝

Ошибка появится **во время выполнения**. А если у нас сложный код, где непонятно, какие аргументы передавать?

**Аннотации помогают избежать таких ситуаций!** Они говорят программисту: *"Эй, эта функция должна получать числа и возвращать число."*

</aside>

<aside>
💡

В Python мы можем указывать типы с помощью **аннотаций**. Это **не строгая проверка**, а лишь "подсказка" для нас и инструментов анализа кода.

</aside>

---

### **Простые аннотации**

```python
def add(a: int, b: int) -> int:
    return a + b

```

- `a: int` обозначает, что `a` должен быть целым числом (`int`)
- `b: int` обозначает, что `b` должен быть целым числом (`int`)
- `-> int` обозначает, что  функция должна вернуть целое число

<aside>
🛠

Посмотрим как это выглядит в редакторе кода

При попытки обращения функции и наведении на нее курсора, появится подсказка:

</aside>

![image.png](%D0%A0%D0%BE%D0%BB%D0%B5%D0%B2%D0%B0%D1%8F%20%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D1%8C%20%D0%B8%20%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F/image%206.png)

<aside>
☝

Здесь отображено то, что мы и указали в параметрах

- Функция ждет, что a и b будут типа `int` , а вернет это функция так же `int`
</aside>

---

### **Аннотации для коллекций**

<aside>
❓

Что если функция принимает список? Используем `List` из модуля `typing`:

</aside>

```python
from typing import List

def sum_numbers(numbers: List[int]) -> int:
    return sum(numbers)

```

<aside>
🛠

Теперь IDE сразу подскажет, что в `numbers` должны быть **только числа**.

</aside>

Или например:

```python
from typing import Dict, Tuple, Set

def user_info() -> Dict[str, int]:
    return {"age": 25, "height": 180}

def get_coordinates() -> Tuple[float, float]:
    return (55.7558, 37.6173)  # Москва: широта, долгота

def unique_numbers(numbers: Set[int]) -> Set[int]:
    return set(numbers)
```

<aside>
💡

Хинт `:list` `:set` `:tuple` `:dict` без необходимости импортировать из `typing` появился в **Python 3.9**.

До этого, в **Python 3.5–3.8**, использовали из `typing`:

</aside>

```python
from typing import List

def func(numbers: List[int]) -> List[int]:
    return numbers

```

<aside>
💡

Начиная с Python 3.9, можно просто писать:

</aside>

```python
def func(numbers: list[int]) -> list[int]:
    return numbers

```

<aside>
⚙

Это стало возможным благодаря **PEP 585**, который позволил использовать встроенные типы (`list`, `dict`, `set`, `tuple`) в аннотациях без `typing.List`, `typing.Dict` и т. д.

Поэтому если на вашем проекте используется Python 3.9 и старше - можете просто пользоваться аннотацией без импорта из модуля typing

</aside>

---

### **Аннотации для классов**

<aside>
➖

Аннотации работают и в классах:

</aside>

```python
class User:
    def __init__(self, name: str, age: int):
        self.name = name
        self.age = age

    def greet(self) -> str:
        return f"Привет, меня зовут {self.name}!"

```

<aside>
☝

Теперь сразу видно, что `name` – строка, а `age` – число. **Код стал самодокументируемым!**

</aside>

---

### **Когда значение может быть `None`**

<aside>
⚙

Если функция может вернуть **значение или `None`**, используем `Optional`:

</aside>

```python
from typing import Optional

def find_user(user_id: int) -> Optional[str]:
    if user_id == 1:
        return "Пользователь найден"
    return None

```

<aside>
☝

**`Optional[str]`** значит, что функция вернёт **либо `str`, либо `None`**.

</aside>

---

### **Использование `Union`**

<aside>
⚙

Если аргумент может быть **разных типов**, используем `Union`:

</aside>

```python
from typing import Union

def process_input(value: Union[int, str]) -> str:
    return f"Ты передал: {value}"

```

<aside>
☝

 **`Union[int, str]`** значит, что можно передавать **и числа, и строки**.

</aside>

---

### 🧪**Практическое задание: Разбираемся с аннотациями в Python**

<aside>
🍄

**Цель**: Понять, как аннотации помогают делать код чище, понятнее и безопаснее.

**Что нужно сделать**: Написать функции с аннотациями, проверить их в IDE и исправить ошибки.

### **1. Простые аннотации**

Создай файл `annotations_practice.py` и реализуй функцию `multiply`, которая принимает **два числа** и возвращает **их произведение**. Добавь аннотации типов.

```python
def multiply(a, b):
    return a * b
```

📌 **Задача**:

1. Добавь аннотации типов (`a: int, b: int -> int`).
2. Запусти код в **PyCharm/VSCode** и попробуй передать строки вместо чисел.
3. IDE должна **выдать предупреждение** – значит, аннотации работают!

---

### **2. Работа с коллекциями**

Добавь функцию, которая принимает **список чисел** и возвращает их сумму.

```python
def sum_numbers(numbers):
    return sum(numbers)

```

📌 **Задача**:

1. Укажи, что `numbers` – это `List[int]`, а функция возвращает `int`.
2. Запусти код и попробуй передать в список **строки** (`["one", "two", "three"]`).
3. Проверь, как IDE реагирует на это.

---

### **3. Используем `Optional`**

Иногда функция может вернуть `None`, если данных нет.

```python
def find_user(user_id):
    if user_id == 1:
        return "Пользователь найден"
    return None

```

📌 **Задача**:

1. Добавь аннотацию, что `user_id` – это `int`, а результат `Optional[str]`.
2. Проверь, что функция **не ломается**, если вернуть `None`.

---

### **4. Используем `Union`**

Некоторые функции принимают **разные типы данных**. Например, функция, которая превращает число или строку в строку.

```python
def process_input(value):
    return f"Ты передал: {value}"

```

📌 **Задача**:

1. Сделай так, чтобы `value` мог быть либо `int`, либо `str`.
2. Используй `Union` и проверь, как IDE подсказывает правильные методы для каждого типа.

---

### **5. Работа с классами**

Создай класс `User`, который хранит **имя** и **возраст** пользователя.

```python
class User:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def greet(self):
        return f"Привет, меня зовут {self.name}!"

```

📌 **Задача**:

1. Добавь аннотации для `name` (строка) и `age` (число).
2. Укажи, что метод `greet` возвращает `str`.
3. Создай объект `User("Артем", 25)`, вызови `greet()` и убедись, что всё работает.

---

### **🚀 Дополнительный челлендж**

1. **Создай функцию** `get_even_numbers`, которая принимает **список чисел** и возвращает **только чётные числа**.
2. Используй `List[int]` в аннотациях.

```python
def get_even_numbers(numbers):
    return [num for num in numbers if num % 2 == 0]

```

📌 **Попробуй передать в список строки и посмотри, как IDE реагирует на ошибку.**

</aside>

---

### **Что такое нотация? (доп инфа просто держу в курсе)**

<aside>
💡

**Нотация** – это система обозначений, которая используется для передачи информации в удобном и структурированном виде. Она встречается во многих сферах: в математике, программировании, лингвистике, музыке и даже химии.

</aside>

---

### **Различия между аннотацией и нотацией**

| Термин | Определение |
| --- | --- |
| **Аннотация** | Дополнительная информация в коде, например, подсказки типов (`type hints` в Python). |
| **Нотация** | Система записи данных или правил, например, `Big-O` в алгоритмах, `CamelCase` в именовании переменных. |

---

### **Виды нотаций в программировании**

### **1. Нотация для именования переменных и функций**

<aside>
➖

**CamelCase** – `userProfile`, `getUserData()`

**snake_case** – `user_profile`, `get_user_data()`

**PascalCase** – `UserProfile`, `GetUserData()`

**kebab-case** (используется в URL и CSS) – `user-profile`, `get-user-data`

**Пример:**

В Python принято использовать `snake_case` для переменных и `CamelCase` для классов.

</aside>

```python
class UserProfile:  # PascalCase для классов
    def get_user_data(self):  # snake_case для методов
        pass

```

---

### **2. Математическая нотация в программировании**

<aside>
➖

**Big-O (O-нотация)** – используется для оценки сложности алгоритмов.

**Σ-нотация (сумма)** – для записи циклов.

**Λ-нотация (лямбда-выражения)** – используется в функциональном программировании.

**Пример Big-O:**

Алгоритм с линейной сложностью `O(n)`:

</aside>

```python
def find_element(lst, target):
    for item in lst:
        if item == target:
            return True
    return False

```

---

### **3. JSON (JavaScript Object Notation)**

<aside>
➖

Используется для обмена данными между клиентом и сервером.

</aside>

```json
{
  "name": "Артем",
  "age": 25,
  "skills": ["Python", "QA"]
}

```

---

<aside>
✅

### **Итог**

- **Нотация** – это система записи данных и правил.
- В программировании бывают **нотации для именования, алгоритмов и форматов данных**.
- Аннотация – это **дополнительная информация** в коде, например, подсказки типов в Python.
</aside>

<aside>
💭

Если ты спрашивал именно про **аннотации в Python**, смотри предыдущее объяснение 😉

</aside>

---

### `.env` или храним секреты правильно!

### **Что такое `.env` и как с этим работать? на следующем уроке добавим к себе .Env**

<aside>
⚙

`.env` – это файл, в котором хранятся **переменные окружения** (environment variables). Они используются для хранения **конфиденциальных данных**

</aside>

<aside>
❓

**Зачем нужен `.env`?**

- **Безопасность** – убираем секретные данные из кода.
- **Гибкость** – можем менять конфигурацию без изменения кода.
- **Портативность** – один `.env` для разных сред (локальная, тестовая, прод).
</aside>

---

### **Создание `.env` файла**

<aside>
🛠

В корне проекта создаем файл `.env` и записываем в него переменные

Давайте поместим туда креды нашего супер админа

</aside>

```
SUPER_ADMIN_USERNAME = 'api1@gmail.com'
SUPER_ADMIN_PASSWORD = 'asdqwe123Q'
```

---

### **Чтение `.env` в Python**

<aside>
🛠

Чтобы загрузить `.env` в коде, используем библиотеку **python-dotenv**:

</aside>

**Установим библиотеку:**

```bash
pip install python-dotenv

```

<aside>
⚙

**Пример использования `.env` в Python:**

Чтобы теперь у нас креды не лежали прямо в коде, сделаем так, чтобы они забирались из этого файла. Для этого перейдем в resources/user_creds.py

</aside>

```python
import os
from dotenv import load_dotenv

load_dotenv()

class SuperAdminCreds:
    USERNAME = os.getenv('SUPER_ADMIN_USERNAME')
    PASSWORD = os.getenv('SUPER_ADMIN_PASSWORD')

```

<aside>
⚙

- `load_dotenv()` - здесь мы загружаем наши переменные из .env
- `os.getenv` - забираем значение наших переменных из .env
</aside>

---

### **Защита `.env`: игнорируем в Git**

<aside>
💡

Чтобы случайно не отправить секретные ключи в репозиторий, добавляем `.env` в `.gitignore`:

</aside>

```
.env
```

<aside>
⚙

Теперь этот файл не будет индексироваться, когда вы будете писать `git add .` , и соответственно не улетит в удаленный репозиторий

</aside>

---

### 🧪Практика

<aside>
🍄

Написать тест, который проверят удаление фильмов но с ролевой моделью, по доке только супер админы могут удалять так же он должен быть параметризован

</aside>